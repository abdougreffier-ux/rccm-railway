# ── Configuration Nixpacks pour Railway ──────────────────────────────────────
# Nixpacks détecte automatiquement Python/Django via requirements.txt.
# Les polices (DejaVu, Noto) ne sont pas installées ici pour éviter les
# conflits avec la détection automatique. Les PDFs utiliseront les polices
# système par défaut (fonctionnel pour les tests, à améliorer en production).
#
# La commande de démarrage est définie dans railway.json (startCommand).
# Ne pas ajouter de section [start] ici — elle serait ignorée ou en conflit.
