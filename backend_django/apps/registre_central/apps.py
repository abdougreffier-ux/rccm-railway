from django.apps import AppConfig


class RegistreCentralConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.registre_central'
    verbose_name       = 'Registre Central — Consolidation nationale'
