from django.apps import AppConfig


class InteropConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.interop'
    verbose_name       = 'Interopérabilité — API inter-administrations'
