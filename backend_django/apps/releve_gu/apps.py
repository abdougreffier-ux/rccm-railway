from django.apps import AppConfig


class ReleveGuConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.releve_gu'
    verbose_name       = 'Relevé mensuel Guichet unique'
